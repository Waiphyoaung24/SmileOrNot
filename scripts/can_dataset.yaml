# Ultralytics dataset config for canspector (1-class can detector).
# Upload data/can/yolo/ + this file to Ultralytics Platform; train YOLO11n or YOLO26n.
path: ../data/can/yolo
train: images/train
val: images/val
test: images/test

names:
  0: can
